export class Palestra {

    id: number;
    nome: string;
    resumo: string;

    constructor(id: number, nome: string, resumo: string) {
        this.id = id;
        this.nome = nome;
        this.resumo = resumo;
    }
}